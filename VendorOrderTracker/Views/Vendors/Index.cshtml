@using VendorOrderTracker.Models;

<h2>Vendors List</h2>

@if (Model.Count == 0)
{
    <p>No Vendors have been added.</p>
}

@foreach (Vendor vendor in Model)
{
    <a href='/vendors/@vendor.Id'>@vendor.Name</a><br>
}

<a href='vendors/new'>Create New Vendor</a>


