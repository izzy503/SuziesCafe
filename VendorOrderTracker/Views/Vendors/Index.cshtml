@using VendorOrderTracker.Models;

<h2>Vendors List</h2>

@if (Model.Count == 0)
{
    <p>No Vendors have been added.</p>
}

<ul>
@foreach (Vendor vendor in @Model)
{   
    <li>
    @vendor.Name
    <a href='/vendors/@vendor.Id/orders'> View Orders</a><br>
    </li>
}
<ul>

<a href='vendors/create'>Create New Vendor</a>


